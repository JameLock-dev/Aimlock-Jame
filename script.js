const appShell=document.getElementById("appShell");const loginOverlay=document.getElementById("loginOverlay");const loginKeyInput=document.getElementById("loginKeyInput");const toggleKeyBtn=document.getElementById("toggleKeyBtn");const activateBtn=document.getElementById("activateBtn");const loginStatus=document.getElementById("loginStatus");const freeKeyBtn=document.getElementById("freeKeyBtn");const contactKeyBtn=document.getElementById("contactKeyBtn");const keyExpireText=document.getElementById("keyExpireText");const keySlotText=document.getElementById("keySlotText");const onlineCount=document.getElementById("onlineCount");const keyActive=document.getElementById("keyActive");const todayCount=document.getElementById("todayCount");const railwayStatus=document.getElementById("railwayStatus");const updateTime=document.getElementById("updateTime");const toast=document.getElementById("toast");function pad(n){return String(n).padStart(2,"0")}function nowTime(){const n=new Date();return`${pad(n.getHours())}:${pad(n.getMinutes())}:${pad(n.getSeconds())}`}function showToast(msg){toast.textContent=msg;toast.classList.add("show");setTimeout(()=>toast.classList.remove("show"),2200)}function lockApp(lock){if(lock){appShell.classList.add("locked");loginOverlay.classList.remove("hidden")}else{appShell.classList.remove("locked");loginOverlay.classList.add("hidden")}}function deviceId(){let id=localStorage.getItem("deviceId");if(!id){id="DEV-"+Math.random().toString(36).slice(2,10).toUpperCase();localStorage.setItem("deviceId",id)}return id}function formatExpire(expire){const d=new Date(expire);if(Number.isNaN(d.getTime()))return expire||"--";return d.toLocaleString("vi-VN",{hour:"2-digit",minute:"2-digit",second:"2-digit",day:"2-digit",month:"2-digit",year:"numeric"})}async function verifyKey(){const key=loginKeyInput.value.trim();if(!key){loginStatus.textContent="Vui lòng nhập Password / Key.";loginStatus.className="login-status error";showToast("Thiếu key");return}loginStatus.textContent="Đang kiểm tra Postgres key server...";loginStatus.className="login-status";activateBtn.disabled=true;activateBtn.textContent="Đang kích hoạt...";try{const res=await fetch("/api/verify-key",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({key,deviceId:deviceId()})});const data=await res.json();if(!data.ok)throw new Error(data.message||"Key không hợp lệ.");localStorage.setItem("jameLoginUnlocked","true");localStorage.setItem("jameKeyInfo",JSON.stringify(data.key));keyExpireText.textContent=formatExpire(data.key.expire);keySlotText.textContent=`${data.key.slotUsed}/${data.key.slotLimit}`;loginStatus.textContent="Key hợp lệ. Đang vào app...";loginStatus.className="login-status success";showToast("Đăng nhập thành công");setTimeout(()=>lockApp(false),650);updateStats()}catch(err){loginStatus.textContent=err.message||"Key không hợp lệ. Vui lòng thử lại.";loginStatus.className="login-status error";showToast("Key không hợp lệ")}finally{activateBtn.disabled=false;activateBtn.textContent="⚡ Kích Hoạt Jame"}}toggleKeyBtn.addEventListener("click",()=>{const isPass=loginKeyInput.type==="password";loginKeyInput.type=isPass?"text":"password";toggleKeyBtn.textContent=isPass?"🙈":"👁"});activateBtn.addEventListener("click",verifyKey);loginKeyInput.addEventListener("keydown",e=>{if(e.key==="Enter")verifyKey()});freeKeyBtn.addEventListener("click",()=>window.open("https://www.tiktok.com/@jame.ff.11","_blank","noopener"));contactKeyBtn.addEventListener("click",()=>showToast("Liên hệ Zalo: 0333635135"));async function updateStats(){try{const res=await fetch("/api/stats");const d=await res.json();onlineCount.textContent=d.online??0;keyActive.textContent=d.activeKeys??0;todayCount.textContent=d.today??0;railwayStatus.textContent=d.railway||"Online"}catch{railwayStatus.textContent="OFFLINE"}updateTime.textContent=nowTime()}const savedInfo=JSON.parse(localStorage.getItem("jameKeyInfo")||"null");if(localStorage.getItem("jameLoginUnlocked")==="true"&&savedInfo){keyExpireText.textContent=formatExpire(savedInfo.expire);keySlotText.textContent=`${savedInfo.slotUsed}/${savedInfo.slotLimit}`;lockApp(false)}else lockApp(true);updateStats();setInterval(updateStats,4000);